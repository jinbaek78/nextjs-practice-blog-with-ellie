// import { AiTwotoneCalendar } from 'react-icons/ai';
// import PostsData from '../../public/data/posts.json';
// import { render, screen } from '@testing-library/react';
// import PostContent from '@/components/PostContent';

// import MarkdownViewer from '../../src/components/MarkdownViewer';
// jest.mock('../../src/components/MarkdownViewer');
// jest.mock('react-icons/ai');

// describe.skip('PostContent', () => {
//   const post = { ...PostsData[0], content: 'test', next: null, prev: null };
//   const { title, content, description, date } = post;

//   afterEach(() => {
//     (MarkdownViewer as jest.MockedFunction<typeof MarkdownViewer>).mockReset();
//     (
//       AiTwotoneCalendar as jest.MockedFunction<typeof AiTwotoneCalendar>
//     ).mockReset();
//   });

//   it('renders with post detail', () => {
//     render(<PostContent post={post} />);

//     expect(screen.getByText(title)).toBeInTheDocument();
//   });
// });

describe.skip('PostContent', () => {
  it('pass for a while', () => {});
});
